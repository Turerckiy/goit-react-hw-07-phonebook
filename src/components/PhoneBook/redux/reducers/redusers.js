// export const addContactReduser = (state = [], actions) => {
    // switch (actions.type) {
    //   case types.ADD_CONTACT:
    //     return [...state, actions.payload];
  
//       case types.DELETE_TODO:
//         const filtered = state.filter((item) => item.id !== actions.payload.id);
  
//         console.log("actions.payload..id", actions.payload.id);
//         return filtered;
  
//       case types.DELETE_ID_FROM_STATE:
//         console.log("actions.payload DELETE_ID_FROM_STATE", actions.payload.id);
//         return state.filter((item) => item.id !== actions.payload.id);
  
//       case types.CHANGE_TODO:
//         return state.map((item) =>
//           item.id !== actions.payload.id
//             ? { ...item, text: actions.payload.text }
//             : item
//         );
  
      // default:
      //   return state;
    // }
  // };
// 